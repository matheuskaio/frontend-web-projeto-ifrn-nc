import React from 'react';
// import PropTypes from 'prop-types';

// import Header from '../../components/Header';

// import { Wrapper } from './styles';

export default function Tcc({ children }) {
  return <h1>Tcc</h1>;
}

// Tcc.propTypes = {
//   children: PropTypes.element.isRequired,
// };
